# dependencies
node_modules/

# sidecar logs
*.log

# per-project runtime state (workpackage manifest, journals, backlog, sessions)
.tandem/


# OpenCode writes its project config here when effort/permissions change
config.json
